name: login
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  path: any
  flutter:
    sdk: flutter

  common_dependencies:
    path: ../../commons/common_dependencies

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  mocktail:

flutter:
  assets:
    - ../../../.env
